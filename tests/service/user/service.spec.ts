import sinon from 'sinon';
// import { expect } from 'chai';

describe('Service', () => {
  const sandbox = sinon.createSandbox();

  beforeEach(() => {

  });

  afterEach(() => {
    sandbox.reset();
  });

  describe('...', () => {
    it('...');
  });
});
